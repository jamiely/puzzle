{
  "name": "puzzle-maker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:run": "vitest run",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@vitest/ui": "^1.0.0",
    "jsdom": "^23.0.0",
    "prettier": "^3.0.0",
    "vitest": "^1.0.0"
  }
}
